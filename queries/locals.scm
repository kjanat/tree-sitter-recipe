; Local scope / binding queries.
; Prescriptions have no variables or scopes — this is a declarative notation,
; not a programming language. File reserved for consistency with tree-sitter
; conventions; may see use if a future pragma introduces named ingredients.
